# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# OS
.DS_Store
Thumbs.db

# Training artifacts
runs/
wandb/
mlruns/

# Model weights (download or train locally; do not commit)
*.pt
*.pth
*.onnx
*.engine

# Temporary captures / debug images at repo root
temp_capture.png

# Realtime capture outputs
rounds/
debug_cells/
dataset/

# PyInstaller / py2app build artifacts (BUILD.md describes the build flow)
build/
dist/
*.app/
*.icns~
# NB: ``xocdia.spec`` is hand-written + committed; only ignore generated
# spec files that PyInstaller creates next to scripts.
*.spec
!xocdia.spec
